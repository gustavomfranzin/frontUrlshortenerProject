function doShorten(event:FormEvent){

  event.preventDefault();
  
    return fetch('http://localhost:5000/shorten', {method: 'POST', body: JSON.stringify({ "originURL": url })}).then((response:any) =>{
           setShortURL(response.data.shortURL) 
         }
       ).catch((error:any)=>{
          console.log(error)
     })
}